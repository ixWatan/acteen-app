<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Meetworkshop" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#F9DD39</item>
        <item name="colorPrimaryVariant">#F9DD39</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:textColorHint">@color/gray</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/Black</item>
        <item name="android:fontFamily">@font/nunito_font</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Meetworkshop.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Meetworkshop.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="Theme.Meetworkshop.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ImageView.Round" parent="">
        <item name="cornerSize">50%</item>
    </style>
    <style name="DialogAnimationSlideIn" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>
    <style name="DialogAnimationSlideOut" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>
</resources>