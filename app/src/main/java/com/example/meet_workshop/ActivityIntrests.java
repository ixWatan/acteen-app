package com.example.meet_workshop;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;

public class ActivityIntrests extends AppCompatActivity {
    boolean color = false;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_intrests);
        Button myButton = (Button) findViewById(R.id.my_button);
        Button myButton1 = (Button) findViewById(R.id.my_button1);
        Button myButton2 = (Button) findViewById(R.id.my_button2);
        Button myButton3 = (Button) findViewById(R.id.my_button3);
        Button myButton4 = (Button) findViewById(R.id.my_button4);
        Button myButton5 = (Button) findViewById(R.id.my_button5);
        Button myButton6 = (Button) findViewById(R.id.my_button6);
        Button myButton7 = (Button) findViewById(R.id.my_button7);
        Button myButton8 = (Button) findViewById(R.id.my_button8);
        Button myButton9 = (Button) findViewById(R.id.my_button9);
        Button myButton10 = (Button) findViewById(R.id.my_button10);
        Button myButton11 = (Button) findViewById(R.id.my_button11);
        Button myButton12 = (Button) findViewById(R.id.my_button12);
        Button myButton13 = (Button) findViewById(R.id.my_button13);
        Button myButton14 = (Button) findViewById(R.id.my_button14);
        Button myButton15 = (Button) findViewById(R.id.my_button15);
        myButton.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton1.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                // Change the background color of the button
                if(color) {
                    myButton1.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton1.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton2.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton2.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton2.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton3.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton3.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton3.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton4.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton4.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton4.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton5.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton5.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton5.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton6.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton6.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton6.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton7.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton7.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton7.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton8.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton8.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton8.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton9.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton9.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton9.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton10.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton10.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton10.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton11.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton11.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton11.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton12.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton12.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton12.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton13.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton13.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton13.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton14.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton14.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton14.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });

        myButton15.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick (View v){
                // Change the background color of the button
                if(color) {
                    myButton15.setBackgroundColor(getResources().getColor(R.color.green));
                } else {
                    myButton15.setBackgroundColor(getResources().getColor(R.color.gray));
                }
                color = !color;
            }
        });
    }

}


